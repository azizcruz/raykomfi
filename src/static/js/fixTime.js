function fixTime() {
  // $(".created-time").each((i, elem) => {
  //   let fixedTime = `منذ ${
  //     elem.innerHTML.replace(/مضت|منذ/g, "").split("،")[0]
  //   }`;

  //   let timeNum = fixedTime.split(" ")[2];

  //   if(timeNum < 11) {
  //       fixedTime = fixedTime.replace(/دقيقة/g, 'دقائق')
  //   }

  //   if (fixedTime.indexOf("أسبوعين") > 0) {
  //     fixedTime = "منذ أسبوعين";
  //   }

  //   if (fixedTime.indexOf("سنتين") > 0) {
  //     fixedTime = "منذ سنتين";
  //   }

  //   if (fixedTime.indexOf("يومين") > 0) {
  //     fixedTime = "منذ يومين";
  //   }

  //   if (fixedTime.indexOf("دقيقتين") > 0) {
  //     fixedTime = "منذ دقيقتين";
  //   }

  //   if (fixedTime.indexOf("ثانيتين") > 0) {
  //     fixedTime = "منذ ثانيتين";
  //   }

  //   if (fixedTime.indexOf("الآن") > 0) {
  //     fixedTime = "الآن";
  //   }
  //   elem.innerHTML = fixedTime;
  // });
}
